/**
 * Who built the system. One constant, so the credit reads the same everywhere
 * it appears: the public site's footer and the sign-in screen. School-issued
 * documents deliberately carry no vendor mark — a report card is the school's
 * paper, not an advertisement.
 */
export const VENDOR_NAME = "Mills Technologies";
